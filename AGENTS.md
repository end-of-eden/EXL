# 홈페이지 작업 지침

## 아이콘

- 새 아이콘을 추가하거나 기존 아이콘을 바꿀 때는 [Lucide](https://lucide.dev/icons/)에서 가져온다.
- 이모지, 문자 기호, 다른 아이콘 라이브러리를 일반 UI 아이콘 대신 사용하지 않는다.
- 기존 `img/icons/lucide/` SVG를 먼저 재사용한다. 필요한 아이콘이 없으면 공식 Lucide 배포본에서 받아 이 폴더에 추가하고 라이선스를 유지한다.
- 스타일은 `css/lucide-icons.css`에서 관리한다. 색상은 `currentColor`를 사용해 라이트·다크 테마를 따른다.
- 아이콘만 있는 버튼에는 기능을 설명하는 `aria-label`을 둔다.
- X 같은 브랜드 로고는 기존 공식 로고를 유지한다.
